import { AnimefilterPipe } from './animefilter.pipe';

describe('AnimefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AnimefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
